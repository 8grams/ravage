{
    admin off
    servers {
        protocols h1 h2
    }
}

${APP_URL} {
    log {
        output file /var/log/caddy/access.log
        format json
    }

    reverse_proxy / localhost:8080

    handle_path /data/* {
        root * /opt
        file_server browse
    }
}
