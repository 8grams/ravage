{
    admin off
    servers {
        protocols h1 h2
    }
}

${APP_URL} {
    log {
        output file /var/log/caddy/access.log
        format json
    }

    handle /data/* {
        root * /opt
        file_server browse
    }

    reverse_proxy localhost:8080
}
